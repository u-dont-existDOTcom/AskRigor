# AskRigor system alignment — Work implementation only

Task ID: askrigor-system-alignment-20260920
Design authority: ASKRIGOR-SYSTEM-ALIGNMENT-20260920-V1
Status at authoring: READY FOR CONTROLLER ADMISSION; not an execution or deployment receipt.

## Execute this task

Read and implement the complete Chat-authored contract at `docs/superpowers/specs/2026-09-20-system-alignment-implementation-contract.md` from commit `eff4fd9a73d00ba904ce13327b04a2fe13c990bd` in `u-dont-existDOTcom/AskRigor`. Its exact UTF-8 SHA-256 is `dcab9db93adda89cf145f5c5f62a18fe3926b1811938e44adea3c9349fbfd685` (23,946 bytes). Read all of it before implementation. This entrypoint does not replace or summarize away that contract.

Retrieve current Universal AGENTS.md first, then current AskRigor governance and only the rules triggered by this task. The public design baseline is `59dcc0acdf207ba71a870e50ca4ef66036da3342`; compare current main before creating an isolated implementation branch. Do not work in an existing dirty or frozen-evaluation checkout. Never reset, overwrite, force-push, or borrow another worker's mutable branch. Coordinate overlaps through Mission Control.

The private controller launch envelope supplies the exact originating Chat title and URL, requested native Work title, source-message provenance, and admitted task/worker bindings. Preserve them in every owner-facing receipt. Do not invent missing identity or fill trusted receipt fields with this document's text. A public file intentionally omits private chat locators.

## Selected design; no additional architecture delegated

Implement the existing atomic protocol snapshot in the legacy loader; immutable public runtime bundle and lossless bounded continuation; whole-bundle session binding; deterministic public adapter/reference generation; explicit legacy/standard-v2/Gemini capability profiles; the read-only load_research_runtime operation; scoped discovery/error/report composition repairs only where tests reproduce a gap; and package/source/installed-consumer consistency and rollback preparation.

Keep YouTube primary when relevant. Preserve all currently canonical clinical semantics and the exact XML hashes stated in the contract. Do not hardcode remedies, invent new research quotas, discard partial evidence, conflate platform failure with no evidence, or silently execute excluded research sources. Missing public operations must be mapped to actual supported paths, not fabricated calls.

Use the current prompt-led research product and existing controlled research machinery. Do not revive the separately deprioritized executable epistemic-verifier campaign, add another orchestrator, or use a new database/vector store/model judge.

## Phases

P0. Record source baseline, isolation, relevant lesson-queue checkpoint and actual consumer/capability inventory. Treat an unavailable lesson checkpoint as unavailable, not zero. Establish the existing test-cost observer. Add reproducing tests against unchanged code.

P1. Implement atomic loading, immutable runtime bundle/chunking, and whole-bundle session binding. Preserve old calls and old-session compatibility or explicit drift boundaries. Return a factual phase receipt through Mission Control; the review boundary is nonterminal.

P2. Implement the deterministic generated adapter/references and public runtime operation/profile bindings. Test exact source propagation, packaged dependency closure, private-boundary exclusion and public/internal/host capability separation. Preserve unrelated packaged skills.

P3. Repair only demonstrated discovery, source-scope, error-state, liveness and report-composition defects. Test broad unseeded prompts, newly discovered comment candidates, negative/mixed episodes, community-only requests, unavailable transcripts and independent executable lanes. Do not mistake a string-presence test for observed model behavior.

P4. Build the release candidate and verify package identities/rollback locally. Use disposable or explicitly authorized test consumers; do not replace an installed production consumer or deploy the backend. Where current live access is unavailable, complete local evidence and record only that acceptance boundary.

P5. Run the complete review-boundary repository gate, inspect the exact final diff, create/update the isolated implementation PR, and send the final factual evidence packet through Mission Control for Chat-owned review. This task ends in a reviewed implementation candidate, not an unauthorized production release.

The complete contract's A01–A20 acceptance cases govern every phase. Record each as passed, failed, not run, or access-blocked with exact evidence. Reuse existing tests and mechanisms when they already satisfy the case; no duplicate framework is required.

## Execution budget and tactical freedom

Use focused/affected tests during implementation. Run `npm run verify` at the actual review boundary, plus repository-required checks applicable to the changed surface. Use Node as declared by the current repository. No repeated green full suites, broad mutation campaign or multi-model tournament without a decision-changing reason.

Work owns code, ordinary tactical implementation choices, test execution, logs, repairs and packaging. Chat retains policy, methodology, architecture changes, semantic acceptance, prioritization changes, publication and release decisions.

On a surface exposing model/effort, Sol Medium is expected sufficient for bounded generator/adapter edits. Genuinely difficult cross-system integration follows the active owner routing policy (currently Sol Extra High baseline). Native ChatGPT Work does not receive Codex-only model/thinking setters. Record an unobservable setting honestly; do not invent effective-model attestation. Missing permission, context or transport is not a reasoning-effort failure.

No paid model API, new paid infrastructure, purchased credits, production deployment, database migration, credential rotation, global permission change, authentication weakening, or frozen MAST/validation access. A merge that would auto-deploy remains outside this task. No raw private health incident, source conversation, credentials or raw community corpus may enter GitHub, public fixtures, logs or analytics.

## Closed-loop Mission Control behavior

Use the existing single-writer controller, source-bound execution-directive admission, native Work dispatcher, worker channel and fleet watch. Register this as its own AskRigor task; never overwrite the ongoing Mission Control development worker's owner outcome.

The controller must supply or create an authorized dedicated MISSION_CONTROL_ONLY reasoning supervisor. The originating personal Chat is a backlink, not an automated relay destination. A dashboard link alone is not supervisor registration.

For each meaningful phase: publish factual receipt -> controller routes to the registered reasoning supervisor -> supervisor reads evidence and returns a source-bound continuation -> controller resumes the same verified Work task. Keep waiting/recovery state durable; do not ask the owner to paste results or type continue. Do not let Work approve its own scientific/policy result. Continue independent authorized work while one lane waits.

Use idempotent dispatch IDs, exact directive revisions/digests and persistent native Work locators. After an ambiguous send, recover the existing task before another mutation. Preserve actual product approval, stale-lease fencing, host ownership, scope and spending gates. A transport-only sidecar acknowledgement is not semantic acceptance or execution.

Use the existing native periodic supervision and bounded same-chat recovery. Roll over the reasoning supervisor around 10–15 substantial turns or earlier on material context/authority drift, carrying source contract, completed evidence, pending decisions and next executable action. Do not create another scheduler or a parallel uncontrolled polling daemon.

The controller may call the task self-running only after observing native Work dispatch, a real factual execution receipt, a source-bound supervisor response, and same-task continuation, with periodic supervision armed. Until then retain distinct QUEUED, DISPATCH_BLOCKED, PENDING_PRODUCT_APPROVAL or PENDING_SETUP state. Do not forge provider identity, source-message identity, a nonce, a trusted setter receipt or mayExecute permission to get through a gate.

## Receipts and stop conditions

Every phase receipt includes exact source and task identity; branch/head; files changed; commands and exit statuses; focused test results; direct-endpoint evidence; A01–A20 status; deviations; precise unresolved boundaries; and the next implementation action already determined by this contract. Private runtime copies carry the exact originating-Chat backlink.

Final packet includes diff/PR, generated package/source manifests, compatibility results, observed consumer acceptance or scoped access failures, rollback materials, test-cost evidence, privacy checks and separate source/operational/behavioral/release states. Do not report full clinical review, efficacy findings, installed acceptance or autonomous execution without evidence.

A review boundary is not task completion. Route policy conflicts, necessary design changes and acceptance decisions to Chat via Mission Control. Repair routine engineering failures without asking the owner. Stop only the affected action for a genuine new authority boundary or unavailable external capability after independent work is complete. Report the exact missing actor/interface; never convert it into a vague request for the owner to manage implementation.

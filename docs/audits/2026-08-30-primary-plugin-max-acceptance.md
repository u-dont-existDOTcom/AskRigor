# Primary regular-Chat AskRigor Max acceptance

Date: 2026-08-30

Surface: the owner's primary Brave profile, ordinary ChatGPT, with the
installed **AskRigor** plugin opened through its `Try in chat` action. The
fresh chat visibly showed **5.6 Sol** at **Max** reasoning effort.

## Installed-plugin readback

The ChatGPT plugin directory listed AskRigor under Installed. Its detail page
showed the product name `AskRigor`, description `Better research`, version
`1.0.0`, and a `Try in chat` action. The public developer label remained the
generic `App developer`; OpenAI business verification and public plugin
submission remain incomplete and are not inferred from this installation.

The detail page did not expose the complete tool inventory. Exact 21-tool
catalog evidence therefore remains the separate direct production MCP receipt;
this product-interface check tests attachment and invocation, not catalog
enumeration.

## Fresh primary acceptance

The fixed read-only prompt required `get_protocol_manifest` for Universal and
HRP plus `fetch_pubmed_record` for PMID `40223676`, and prohibited research
session creation and mutating tools. The response completed after a visible
`Worked for 20s` interval and returned:

- Universal `20.5.15`, revised `2026-08-24`, SHA-256
  `69c5186862ade61d6a97dc842b8c027324c7e2f3fd7147064a360049e0d25172`,
  with successful manifest access;
- HRP `20.5.23`, revised `2026-08-24`, SHA-256
  `bf2adc1c4daea8241c47b2a111d4a19e6bf7427a6401ecf1b3ba75a58e046299`,
  with successful manifest access; and
- PMID `40223676` as `api_visible_complete`, with PubMed metadata and abstract
  returned and no full-text claim.

All values match canonical source and direct production receipts. This is a
passing current primary-account plugin connectivity and bounded retrieval
acceptance, not a substantive research-quality benchmark.

## Custom GPT control and second-account boundary

A separate fresh AskRigor Custom GPT conversation received the same prompt and
correctly refused rather than inventing results because neither requested tool
was available in that session. That negative control confirms the owner's
surface distinction: ordinary ChatGPT with the attached plugin is the primary
acceptance path; the Custom GPT is not evidence of current plugin availability
or achievable reasoning quality.

At this checkpoint, browser control exposed only the primary `Personnel` Brave
profile. The owner's `2nd gpt` profile was not connected to the controlled
browser instance, so no fresh second-account recheck is claimed. The prior
second-account receipt in
`docs/audits/2026-08-29-second-account-plugin-acceptance.md` remains historical
evidence. No private verification data was used in either product test.

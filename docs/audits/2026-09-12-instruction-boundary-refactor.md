# Instruction-boundary refactor candidate

Owner-approved goal: improve effective reasoning and execution across instruction surfaces without
contradictory authority, duplicate active burden, or private dependencies in public protocols.

The timestamp rule is an owner-validated reliable cross-model fix and is preserved unchanged.
The candidate extends the existing obligation/application machinery, clarifies scoped inheritance,
and keeps original protocol bytes recoverable. It does not claim that a prose gate is mechanical
or that source tests prove installed behavior. New standalone public text carries no internal import.

Exact source hashes, transformations, transfer dispositions, before/after evidence, structural
inventory, and tests are in the Chat-authored execution packet and returned worker receipt.
Full-corpus semantic review, installed-package/editor checks, and live runtime acceptance remain
separate unresolved boundaries. No deployment, paid inference, or frozen-study change is authorized.

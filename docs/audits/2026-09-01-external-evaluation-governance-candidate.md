# External evaluation governance candidate

The exact candidate `46bb99a1de67b14ccadac8563299f370cd94c4fe` (tree `320274b3c74107beb1644a5306a132f5b90fa4a8`) instantiates the provisional benchmark manifest and defect ledger against the merged MAST deterministic preflight and private Terminal-Bench miniature proof.

Both artifact families pass strict Draft 2020-12 validation. Twenty-five cross-artifact bindings pass, four correction/recheck policy mutants fail, no inference ran, and no latent answer was published. The two empty defect ledgers mean that zero defects are currently recorded; they do not certify that either evaluator is defect-free.

Focused evaluation coverage passes 17 tests. The exact candidate passes the complete deterministic gate: 130 test files passed with one declared skip, 1,631 tests passed with six declared skips, typecheck passed, and build passed. Dependency installation reported zero vulnerabilities. Test telemetry recorded one full merge-ready run and zero forced redundant full reruns.

The live universal lesson index was applied at `e9d5b64292d6c6a2dbb64ba9333d617efbf489fa`. Branch-bound admission, exact evaluator provenance, proportional testing, bounded release assurance, and the owner's no-institutional-expansion correction all pass.

Typed claim: `SUBTASK_COMPLETE_PARENT_OPEN`. Operational alignment passes the governance artifact contract pending protected merge. Scientific adequacy passes only for provenance and correction-state controls; no benchmark result, defect-free conclusion, HRP performance conclusion, or clinical claim exists. Release adequacy is pending protected merge and includes no production release.

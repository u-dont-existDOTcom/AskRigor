# Phase 2.2 Bounded Child Candidate Receipt

**Parent activation:** PR #168 / `4556b58e55fc445920f831b7a1451c805283a697`  
**Child:** `rights-verifier-and-sealed-mast-preflight-v1`  
**Exact tested candidate:** `2dab865ae817027c324f689da2aaf738de5c6b30`  
**Candidate tree:** `ede95f5a2ab8d7aa2897ca9e56f8922f2aea003a`

## Outcome

The bounded source-rights, private miniature-verifier, MAST adapter, and sealed deterministic SCT-preflight child passes and is ready for protected review. The canonical PR #168 parent objective and its remaining workstreams stay open. This receipt does not claim a MAST model result, clinical conclusion, public benchmark fixture, external proposal, or production release.

The rights scan selected project-authored fictional reports and structured data. Eight real sources are used only for methods, structural failure patterns, and provenance. No copyrighted article packet, real treatment claim, or identifiable person-level data enters the fixture.

The final miniature truth remains in a mode-0700 directory outside the repository. Two correct implementations passed; eight invalid candidates—including report double-counting, person-time/effect-measure mixing, and invalid comparator cases—failed; six verifier mutants were killed; observed false accepts and false rejects were zero. The public receipt contains hashes and aggregate counts, not the fixture or answers.

The MAST adapter pins the exact upstream source, complete current protocol hashes, `gpt-5.6-sol`, the Responses API, and `xhigh` reasoning. Only system instructions may differ across paired conditions. The upstream 174-item example reproduces its `0.7453` SCT score and `0.9310` expert-set proportion. The spend ceiling is zero and no API request occurred.

## Validation

- `npm run external-evaluation:preflight` — pass
- `npm run external-evaluation:acceptance` — pass
- focused evaluation suite — 14 tests pass
- `npm run verify` on the exact candidate — typecheck pass; 129 test files passed and 1 skipped; 1,628 tests passed and 6 skipped; build pass
- final diff/JSON checks — pass
- lesson queue — 0 open, 0 needing review, 0 accepted-but-unincorporated, 4 incorporated/closed, 0 deletion-eligible

Typed claim: `SUBTASK_COMPLETE_PARENT_OPEN`. Operational alignment passes the bounded contract and is pending protected merge. Scientific adequacy passes only for miniature verifier feasibility; no HRP-performance or clinical claim exists. Release adequacy is pending protected merge and includes no production mutation.

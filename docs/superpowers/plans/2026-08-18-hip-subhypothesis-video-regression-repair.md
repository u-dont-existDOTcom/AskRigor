# Hip Subhypothesis and Video-Match Regression Repair Plan

**Goal:** Prevent a broad hip-treatment review from satisfying discovery and
Forum Signal completion with generic conservative-care, NSAID, and
postoperative-rehabilitation pools while omitting distinct conservative or
adjunct hypotheses and program-matched preoperative PT evidence.

**Baseline:** Follow-up commit
`77665858997e1fc212bb7b134685bd2775c61120` on
`agent/gpt-heterodox-discovery-weighting-20260818`. Its generated Instructions
are 7,799 characters with SHA-256
`8cbc6a3a5741f46e08cb184dfb32277d85a4897aa86e993865bfdc219f1b41d6`.
The owner supplied a fresh product-interface result after installing that
candidate. It executed formal and YouTube retrieval, but collapsed distinct PT
programs, selected generic or postoperative videos, omitted gelatin/collagen
and hydration discovery, and still claimed `HRP-complete`.

## Constraints

- Do not change either canonical protocol XML file.
- Preserve the public non-tailored health boundary, protocol transport,
  17-read/one-write Action surface, continuation rules, consent shell, and
  fail-closed privacy controls.
- Keep generated Instructions within 7,800 characters and the plugin skill
  below 500 words.
- Use only sanitized product evidence. Do not retain raw comments,
  continuation handles, private health details, or credentials.
- Do not claim that collagen/gelatin, hydration, swimming, or a PT program is
  effective merely because it must be discovered and dispositioned.
- Keep deployment, editor installation, and product acceptance separate from
  static repository verification.

## TDD sequence

1. Extend the discovery/weighting matrix so the broad uncued hip case requires
   a discrete intervention-hypothesis ledger, program-matched video selection,
   and a matched-video-or-explicit-no-candidate disposition.
2. Add failing cross-surface assertions for the permanent hip regression:
   gelatin/collagen, hydration, swimming/aquatic exercise, and distinct
   preoperative PT programs cannot be represented by one generic PT/exercise
   or postoperative-rehabilitation video.
3. Run the focused matrix, router, plugin, packet, and release tests and retain
   RED before changing source instructions.
4. Add the smallest executable rule to the Project router, Forum Signal
   module, plugin skill, and generated Custom GPT source. Compact genuine
   duplication to preserve the 7,800-character ceiling.
5. Regenerate the Custom GPT packet and verify that OpenAPI operations and
   canonical protocols remain unchanged.
6. Run the focused suite, `npm run verify`, generated-artifact checks,
   `git diff --check`, lesson status, and final diff review.
7. Update durable state and release evidence with this failed product boundary,
   the local repair identity, and the exact remaining editor retest.

## Acceptance criteria

- The broad hip pathway cannot be complete until each named permanent
  regression hypothesis has matched formal/community discovery or an explicit
  no-candidate/access disposition.
- Generic `PT` or `exercise` evidence cannot stand for distinct program
  components, dose, supervision, disease stage, or preoperative purpose.
- Postoperative rehabilitation videos cannot satisfy preoperative conservative
  care or surgery-avoidance discovery.
- Discovery requirements do not become efficacy claims; evidence lanes and
  uncertainty remain separate.
- Static tests and local verification pass without claiming GPT UI acceptance.

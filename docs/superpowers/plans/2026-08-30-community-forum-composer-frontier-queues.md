# Community Forum composer, frontier, and queue fixtures — execution contract

Date: 2026-08-30
Task ID: `community-forum-composer-frontier-queues-20260830`
Owner-outcome epoch: `1`
Assurance lane: `iteration`, with targeted privacy/role/nonautomation hard gates
Status: `ARTIFACT_READY`

## Owner result and authority

Continue the durable Community Health Forum/Public Lead work queue automatically
from the verified PR #142 foundation. The next bounded result is a synthetic-only
implementation of the progressive structured-experience composer state,
balanced public-frontier view models, and separately authorized
moderation/privacy/safety queue behavior with hostile fixtures.

Authority is the exact owner-source receipt, the work-queue next state, the
canonical protocol manifests, and the controlling forum architecture. The
visual design dependency is advisory below product truth. Its live skill was
loaded at `u-dont-existDOTcom/design@02313ab16c40d4eafdb6d8a3597d2796e0ed5e8b`;
two skill-required core files are absent at that head, so no missing guidance is
reconstructed. Superdesign CLI `0.13.0` is available but unauthenticated;
generation/credit spend and visual implementation remain outside this
UI-neutral implementation boundary until authorization and design approval.

## Required outcomes

1. A member-controlled progressive composer can stop early, preserves explicit
   missingness, distinguishes self/proxy/source distance, and cannot create a
   lead without affirmative opt-in.
2. The proposed public paraphrase and provenance/source-distance label are
   reviewable before a synthetic publication request.
3. The synthetic frontier view model exposes benefit, harm, no-effect, mixed,
   and unknown directions without positive-first default ranking, naive
   effectiveness percentages, or popularity-driven evidence upgrades.
4. Public cards preserve verification, completeness, evidence capability,
   formal-evidence relationship, challenges/corrections/withdrawal, and exact
   lead/public versions as independent fields.
5. Moderation, privacy, scientific annotation, safety, and research stewardship
   remain capability-separated; active-role records are explicit, role
   collisions fail, and no role silently edits member scientific meaning.
6. Serious-harm candidates enter a human-owned queue with no automated
   regulatory reporting. Ordinary posts remain conversation unless the member
   explicitly opts into lead conversion.
7. All executable fixtures remain synthetic. No real account, health report,
   public page, indexing, recruitment, reporting, provider activation, or
   deployment is permitted.

## Objective reconciliation matrix

| Owner requirement | Worker interpretation | Task criterion | Acceptance evidence | Status | Authorized change |
| --- | --- | --- | --- | --- | --- |
| Always continue the work queue | Advance the next durable nonterminal state without asking for an execution mode | New stacked task branch from verified PR #142; work-queue checkpoint updated | Git graph, plan, local receipt; stacked PR pending | Satisfied locally | None |
| Preserve the forum/public-lead goal | Implement the next synthetic foundation, not a substitute product or release | Composer, frontier model, queue capabilities match controlling architecture | Hostile tests, schema/service, migration, and privacy receipts | Satisfied | None |
| Preserve corrected public-lead rule | Keep deidentified reporter-consented secondhand leads distinct from subject-voice narratives | Secondhand path remains possible; narrative approval remains stronger | Prior regression plus 31-test affected Community Forum regression | Satisfied | None |
| Keep visibility independent from evidence | Presentation and queue actions cannot mutate evidence capability/certainty | Exact before/after invariance and balanced-direction tests | Focused tests and 12-check PostgreSQL acceptance | Satisfied | None |
| Continue automatically but respect boundaries | Proceed with local reversible work; hold auth, credit spend, publication, deployment, and real data | UI-neutral implementation advances while optional design generation remains gated | Boundary receipt | Satisfied | None |

## Non-satisfying proxy states

- a static mockup without executable state and hostile tests;
- a view model that hides no-effect/harm or sorts benefit first;
- a generic admin queue with implied cross-role authority;
- a composer that silently converts discussion to evidence;
- green unit tests without the database constraint path;
- a synthetic page treated as a public release;
- PR readiness treated as the root Community Forum outcome.

## Verification strategy

- Start test-efficiency telemetry before the first substantive test run.
- Use focused composer/frontier/queue tests in the inner loop.
- Exercise migration/repository behavior against disposable PostgreSQL at the
  integration checkpoint.
- Run the repository's complete deterministic gate only at the pre-PR boundary.
- Review the complete diff, privacy/source-storage/role changes, generated
  artifacts, and nondeployment state before the completion claim.

## Supervision checkpoint

- bootstrap:
  `u-dont-existDOTcom/universal-dev-architecture@90a230e85f78063080dc627ec36a0237c3234f72`,
  `templates/CURRENT-CODEX-WORKER-SUPERVISION-BOOTSTRAP.md`;
- independent owner source: exact current owner message preserved at
  `docs/audits/2026-08-30-community-forum-composer-frontier-queues-owner-source.txt`;
  no durable external chat-message identifier is exposed;
- worker-to-contract alignment: `GREEN` for the local artifact; GitHub
  reproduction remains pending;
- contract-to-owner alignment: `MATCH`;
- completion claim: `ARTIFACT_READY`;
- operational alignment: `ALIGNED_FOR_SYNTHETIC_SUBTASK`;
- scientific adequacy: `NOT_APPLICABLE` for health-outcome inference;
- release adequacy: `FAIL_CLOSED`;
- root outcome: nonterminal;
- supervision-design feedback: none identified; the missing design-repository
  files are a specialist dependency defect, not a supervision-rule defect.

Reconcile after material discoveries, before database integration, before the
stacked PR, and before any completion claim.

## Local artifact-ready checkpoint

- focused/affected Community Forum regression: 31 tests passed;
- disposable PostgreSQL acceptance: 12 of 12 checks passed, with the owned
  container removed afterward;
- synthetic lab manifest validation: passed with every public/real-data/
  recruitment/reporting boundary false;
- source-complete deterministic gate: 114 files passed and 1 skipped; 1,537
  tests passed and 6 skipped; typecheck and build passed;
- final diff check: no whitespace errors; privacy data map, threat model,
  source-storage policy, release evidence, work queue, and Public Atlas index
  updated;
- release remains fail-closed and the root product outcome remains nonterminal.

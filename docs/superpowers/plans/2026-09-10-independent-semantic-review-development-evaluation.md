# Independent semantic representation review DEVELOPMENT evaluation

Run the source-bound Issue #208 continuation against the review boundary merged by PR #212. Keep the run DEVELOPMENT / DISCOVERY only: no MAST or held-out data, protocol edits, deterministic gate edits, deployment, or production integration.

1. Bind the exact Project Manager directive and verify the compatible `main` ancestry plus unchanged reviewer implementation and prompt.
2. Materialize 24 natural source/initial-state packages without consulting prior per-trial scoring. Obtain one fresh blinded second adjudication per state and a fresh third adjudication for every disagreement with the earlier label.
3. Freeze 11 matched source-constant mutation pairs, then obtain two fresh hard-relevance adjudications per defective state and a third only on disagreement.
4. Freeze 46 opaque candidate packages and deterministic three-replicate order.
5. Run 138 sequential fresh ChatGPT consumer review conversations with GPT-5.6 Sol / Pro, one controlled reusable tab, exact prompt/package bytes, controller-observed provenance, and no content retries.
6. Freeze every raw output before unblinding; validate submissions, score individual and majority performance, and apply the directive's ordered strategy rule.
7. Preserve the complete run, verify the repository, open one PR, require exact-head checks, merge only if the source-bound completion conditions hold, and verify the resulting `main` tree.


# Canonical Reasoning Selection application

Status: source-bound implementation candidate only. This slice may create one
draft pull request. It does not authorize readiness, merge, deployment,
release, provider inference, private research, or spending.

## Authority and fixed scope

- Base commit: `12efd23e69fb753ab47215f197d98204b67bac34`, tree
  `562f45b7b99e77d6d6c68041e0da694f884282e2`.
- Project Manager directive:
  `askrigor-universal-reasoning-selection-project-application-v1`, source
  message `c5498e80-1f2e-4b8e-a902-055a2061223c`, 25,030 UTF-8 bytes,
  SHA-256
  `5b91986bbbe8eae85ce00ed12bbd135ac6ac08346f553e209068179cc120609c`.
- Work only on branch `task/universal-reasoning-selection-20260908` in the
  isolated worktree `/tmp/askrigor-universal-reasoning-selection`.
- Add the settled Reasoning Selection wording as Universal `20.5.20`, its
  compact AskRigor research-router application, and the execution-role
  reminder in root `AGENTS.md`. Preserve HRP and Forum Signal bytes.
- Bind and test existing canonical-source delivery. Do not add a generator,
  change runtime code or dependencies, or claim that source delivery proves
  model compliance.

## Controlled-action admission

The first exact request passed the local policy parser and evaluator, but its
HTTP submission returned `401` because the generic process environment did not
carry the existing `worker:askrigor-mast` identity. The response body was 50
bytes with SHA-256
`d56cbd23243c50e48491ad6a046585bf45b260df60a6d7d3c105798b0784b1cb`.

The Project Manager authorized existing-identity inspection and one recovery
submission in directive
`askrigor-reasoning-selection-existing-identity-recovery-v1`, message
`5512d7cf-0fbb-4780-baba-128a2462526d`, 16,204 UTF-8 bytes, SHA-256
`e415da4eade4f7c4156f98e74b2d30cfc667a56731ccc79ce93d1ff490c97ec2`.
Inspection established that the exact registry entry existed, parsed, had kind
`WORKER`, and authorized `askrigor-mast`; no secret value or derivative was
printed or persisted. The exact 4,161-byte recovery request, SHA-256
`bbc30c128d1784905f3a1d7ab92b9222df964f2de2d7e5967be2a5f7f4ece804`,
passed the local gate, installed parser and evaluator. Request
`admission:askrigor:reasoning-selection-recovery:5512d7cf:12efd23:draft-pr:zero-spend:20260908`
then returned an admitted 922-byte response, SHA-256
`7ad6e5106676c96dcf214ab7ec9291b62c5dcc703d571fe25e189adb2e7c5d2e`,
with `mayExecute: true`, `ALLOW_BOUNDED_EXECUTION`, no owner relay, and no
provider delivery. A local staging mismatch was caught before HTTP and does
not count as a submission.

## Implementation and acceptance

1. Add prospective source and delivery assertions before changing canonical
   bytes. Under Node `24.18.0`, the required four-file negative run failed only
   because the `20.5.20` selector and compact project/agent applications were
   absent: 5 failed and 31 passed across 4 files. Its log is
   `/tmp/askrigor-reasoning-selection-negative.log`, SHA-256
   `2f602c2bb1773184450c584f11801300efd8e02347b1f8049d4d6a9db4f859e0`.
2. Apply exact byte patches with fixed preimage digests. Insert the new
   revision first in `revision_history`; insert the Critical selector between
   `revision_history` and `heuristic_attractor_check`; change only the root
   version/date attributes. Prove inverse removal recovers the base Universal
   SHA-256.
3. Insert the exact compact Project application before section 1 and the exact
   execution-role text before the Chat-to-Work gate. Enforce the supplied text,
   complete-file digest and compactness limits.
4. Update only independent current Universal identity assertions and the
   current README receipt. Preserve historical revisions, digests, negative
   fixtures, stale-tuple rejection, and all existing routing and integrity
   gates.
5. Prove all ten semantic work kinds, the real paginated controlled route and
   the real private capturing executor receive the complete updated canonical
   documents. Preserve BOM, malformed-input, unavailable-source, stale cursor,
   stale receipt, replay, no-tools, drift and finalization failures.

## Validation and publication boundary

- Run focused structure, loader, manifest, MCP, frontier, controlled-route,
  private-executor and adjacent historical-gate tests under Node `24.18.0`.
- Run `npm run verify` once on the final candidate, then `npm run build`,
  `npm run generate:custom-gpt`, and `npm run generate:action-openapi` and
  require no unplanned tracked generator changes.
- Run `git diff --check`; validate XML, exact versions, hashes, word/character
  limits, inverse recovery, unchanged HRP/Forum bytes and the allowed-path
  boundary. Inspect the complete diff against the exact base.
- Recheck the lesson queue, commit, push the exact branch once, create one draft
  PR, and observe hosted checks. Return the exact result to the Project Manager.
  Stop before readiness, merge, deployment, release, provider calls or private
  research unless a later source-bound directive explicitly authorizes them.

## Completed local validation

- The exact four-file post-change comparison passed all 36 tests. The broader
  focused identity, MCP, frontier, router, release-receipt and historical-gate
  sweep passed after updating only its independent current-value assertions.
- `npm run verify` under Node `v24.18.0` passed: typecheck, 153 test files passed
  with 1 skipped, 1,830 tests passed with 6 skipped, and build passed.
- After the successful build, `npm run generate:custom-gpt` and
  `npm run generate:action-openapi` both completed. The tracked status before
  and after was byte-identical, SHA-256
  `05e843b55a7a3d187cc515dfb0bc2bcd8bd1f26b3f03cecd2c359b32949204d2`;
  neither generator owns a derived artifact for this runtime-loaded policy.
- `git diff --check`, the allowed-path audit and independent XML parsing passed.
  Universal is `20.5.20` dated `2026-09-08`, SHA-256
  `5365f5fcb8e9abac0b60a5cbbfa23183cf08018f1c0f6ab9a3bb1e8df87ad9b3`.
  Inverse removal recovers the exact `20.5.19` base SHA-256. Project Instructions
  are 7,751 UTF-8 bytes, 7,737 characters and 886 words, SHA-256
  `58d8c8387e962064a393af1cab7d78391e18dfa78571cbb0372aad8455b7db70`.
  HRP and Forum Signal remain byte-identical to base.
- The lesson queue was available at `2026-09-08T06:28:45.066Z`: 1 open
  candidate, 1 needs review, 0 accepted but not incorporated, 4 incorporated
  or closed, and 0 deletion eligible. No lesson expanded this slice.

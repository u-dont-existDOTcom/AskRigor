# Gemini candidate comment-audit plan

**Goal:** Carry the first accepted compact Gemini candidate handoff through the
bounded AskRigor discussion-audit stage and persist a privacy-safe
`youtube_rediscovery_packet` without issuing a medical or HRP verdict.

## Authority and scope

- Apply `project/PROJECT_INSTRUCTIONS.md` before HRP synthesis and read the
  complete `project/FORUM_SIGNAL_MODULE.md` because firsthand outcomes,
  tolerability, failure, and surgery avoidance are material.
- Load and verify the canonical HRP and Universal protocol identities. Preserve
  any deployed-versus-repository mismatch as an access boundary.
- Treat Gemini classifications and creator summaries as provisional. Select
  discussions by decision usefulness, intervention diversity, stage/outcome
  match, independence, and nonredundancy rather than popularity.
- Retrieve unfiltered API-visible comments and independently paginated replies
  for every selected pool. Continue while `continuation_recommended` is true.
- Do not persist raw comments or identifying commenter data. Return only
  normalized non-identifying leads and bounded acquisition receipts.
- Do not claim Forum Signal or HRP completion. Creator claims require transcript
  evidence or an explicit access boundary.

## Execution ledger

- [x] Record the full module ledger and synthesis gate.
- [x] Load HRP and Universal; verify the repository canonical digests.
- [x] Select the three materially distinct suggested discussions.
- [x] Continue every discussion audit to a terminal provider state.
- [x] Separate firsthand episodes from questions, secondhand reports, creator
  replies, promotional praise, cointerventions, and diagnosis/stage mismatch.
- [x] Create a six-lead `youtube_rediscovery_packet` with counter-signals,
  suggested queries, priorities, and exact access boundaries.
- [x] Record the transcript boundary and withhold creator-content claims.
- [x] Parse the JSON artifact, run repository validation, inspect the final
  diff, and prepare a reviewed local commit without push or deployment.

## Completion boundary

This plan is complete when the three selected discussion audits have terminal
receipts with passing synthesis locks, the non-identifying packet is valid JSON,
the repository gate passes, and the local commit is clean. Formal literature,
grey literature, trials, independent non-YouTube communities, targeted
rediscovery, transcript review, bidirectional transfer, and final HRP synthesis
remain separate future work.

## Verification receipts

- The rediscovery packet parsed as JSON with `status: leads_available`, six
  leads, and nine explicit access boundaries.
- `npm run verify` passed typecheck, 59 test files with one credential-gated
  skip, 979 tests with five declared skips, and build.
- The lesson checkpoint at `2026-08-21T04:23:04.458Z` was available: 1 open
  candidate, 1 needs review, 0 accepted not incorporated, 2 incorporated or
  closed, and 0 deletion eligible. No unreviewed lesson expanded this audit.

# Mayan Roots LLC publisher-alignment plan

**Goal:** Keep **AskRigor** as the product and plugin name while making
**Mayan Roots LLC** the truthful publisher/developer/operator identity everywhere
OpenAI reviewers or users encounter that identity.

## Constraints

- Do not invent a legal suffix, registration number, address, jurisdiction, or
  an already-approved business-verification state.
- Preserve the signed-in 2026-08-30 portal readback as observed evidence:
  individual verification was `Approved`, business verification was `Start`,
  and no plugin draft existed or was created.
- Keep the developer-identity gate open until Mayan Roots LLC is approved in the
  OpenAI portal and the approval has a non-secret receipt.
- Preserve an exact rollback point before public-site deployment and plugin
  reinstall. Do not push, publish a portal draft, or submit for review without
  separate authority.

## Execution and verification

1. Add failing regression tests that require:
   - product/display name `AskRigor`;
   - author/developer identity `Mayan Roots LLC`;
   - the exact public relationship disclosure
     `AskRigor is a product operated by Mayan Roots LLC.` on all four public pages;
   - current privacy/terms effective dates; and
   - an explicit, incomplete Mayan Roots LLC business-verification gate.
2. Update plugin metadata, the submission packet, the four-page static site,
   and current reviewer/recovery documentation without rewriting historical
   evidence.
3. Run focused tests, plugin validation and source receipt, public-site checks,
   the complete deterministic gate, and final-diff review.
4. Commit the reviewed candidate, open a pull request, wait for required checks,
   and merge only when the protected candidate is green and reversible.
5. Build the immutable public-site archive from the exact merge commit, preserve
   the current production release as rollback, deploy only the static site, and
   run direct HTTPS/header/content/MCP regression acceptance.
6. Preserve the prior installed-plugin receipt, apply one cachebuster to the
   exact merged source package, reinstall from the validated personal
   marketplace, and require matching complete eight-member source/installed
   receipts plus catalog, protocol-manifest, and read-only connector probes.
7. Update release/recovery evidence with exact commits, digests, checks, and
   remaining external gates; run the lesson closeout checkpoint.

## Owner/external boundary

The repository and public site can truthfully select Mayan Roots LLC now. OpenAI
business verification, global data residency, domain verification, Scan Tools,
demo upload, final portal review, and submission remain separate external gates.

## Closeout

Source merged through PRs #134 and #135, with exact legal-name merge
`4d4bd43303045223394480b13153e7ae3b9149bd`. The immutable static site is active
as release `4d4bd4330304`; direct body/header/MCP acceptance passed. The personal
plugin was reinstalled as `0.1.0+codex.20260830020029`; complete source and
installed receipts match, and both site and plugin rollback sources remain
available. The developer-identity and other portal gates above remain open.

# AskRigor system alignment implementation

Status: active implementation plan; no deployment or release authorization
Date: 2026-09-22
Task: `askrigor-system-alignment-20260920`
Design: `ASKRIGOR-SYSTEM-ALIGNMENT-20260920-V1`
Design source: commit `eff4fd9a73d00ba904ce13327b04a2fe13c990bd`, `docs/superpowers/specs/2026-09-20-system-alignment-implementation-contract.md`, SHA-256 `dcab9db93adda89cf145f5c5f62a18fe3926b1811938e44adea3c9349fbfd685`
Source baseline: `59dcc0acdf207ba71a870e50ca4ef66036da3342`
Branch: `task/askrigor-system-alignment-20260920-v1-20260922`

## Root outcome and authority

Produce a reviewed, rollback-ready implementation candidate that preserves the prompt-led product while making public runtime delivery lossless, generated projections reproducible, runtime identity whole-bundle-bound, and demonstrated discovery/reporting behavior faithful to the selected design. The owner request and the design above authorize implementation and review only. They do not authorize production installation, merge, deployment, database work, spend, new infrastructure, clinical-policy changes, or use of frozen validation material.

Canonical protocol bytes are immutable in this task:

- `protocols/HRP_Full.xml`: `254759df38934c28b06709dace9fcb266fc9967913be1296de99a461be596816`
- `protocols/Universal_Instructions.xml`: `c869d770ecc13280a40567ba382324e1d9a6b0af7c35165008781f186317d9b2`

The implementation surface does not expose native Work or Mission Control task controls. Local implementation and repository review may continue, but native dispatch, same-task resume, supervisor delivery, and fleet-watch claims remain access-blocked unless direct evidence becomes available.

## Current baseline

- Current local and remote default branch matched the selected source baseline at task start.
- A fresh isolated checkout and uniquely named branch are in use; the stale local checkout was not reused.
- `npm run lessons:status` reported `available`, with one open candidate and one needing review. No unreviewed lesson expands this task.
- Test-cost observation is active for the task.
- Public registry, Action routes, semantic policy loading, controlled session state, Custom GPT generation, package receipts, and relevant open work have been inventoried before edits.
- Runtime/model-effort controls are not observable on this execution surface. This is recorded as unavailable rather than inferred.

## Active lesson contract

| Trigger | Required behavior | Failure signal | Repair | Enforcement |
|---|---|---|---|---|
| Canonical or projected policy changes | Keep one authoritative source per rule and regenerate affected projections | A generated artifact stays green after an authoritative anchor changes | Add source-map/hash closure and deterministic regeneration | A01, A02, generator tests |
| A protocol is loaded for one response | Derive text and manifest from one snapshot | Two loaders can observe different revisions | Consume `loadProtocolSnapshot` once | A04, continuation tests |
| Public runtime is paged | Bind the chain to one immutable release bundle | Mixed revision, reordered, skipped, expired, or tampered pages succeed | Signed bounded continuation over immutable bundle identity | A05, runtime tests |
| A session is created or resumed | Bind protocols and operational policy together | Module-only drift is silently inherited | Persist whole-bundle identity or enter explicit drift | A07, session tests |
| A capability is advertised | Declare its actual public/internal/host/unavailable surface | Internal operation is reported as directly callable | Generate profiles from the registry plus explicit bindings | A03, A06, registry tests |
| A consumer package is generated | Include exact declared public references only | Missing dependency, private path, or stale generated bytes | Deterministic closure check and manifest | A01, A02, A18 |
| Discovery learns a candidate | Preserve unseeded discovery and reopen around new source-grounded candidates | Narrow audit starts first or comment-only candidate disappears | Repair the demonstrated controller/adapter seam only | A10, A11 |
| Mixed or restricted evidence is reported | Preserve failures, uncertainty, combinations, provenance, and scope | Positive-only summary or unsupported clinical/full-HRP label | Reuse bounded-evidence and reconciliation gates | A12–A16 |
| A provider or access boundary fails | Preserve the precise state and independent ready work | Failure is treated as zero results or fake completion | Carry distinct typed boundary and bounded retry | A09, A14, A15 |
| Review artifacts are produced | Keep private source identity, raw incidents, secrets, and frozen data out | Private locator or sensitive corpus appears in diff/log/package | Use synthetic fixtures and public-safe receipts | A18, privacy scan |
| Verification is repeated | Use focused tests in the inner loop and one full review-boundary run | Broad suites run after every edit | Record commands with the approved observer | test-cost report |
| Work or Mission Control is discussed | Report only directly observed lifecycle facts | Local task is called native Work or self-running | Mark unavailable control-plane steps access-blocked | A19, A20 |

## Implementation sequence

### P0 — reproduce and freeze the baseline

1. Add failing tests for the atomic protocol-read defect, deterministic public-generation closure, public profile mismatch, runtime continuation, and whole-bundle session drift.
2. Reuse existing behavioral tests where they already exercise A08–A16; add fixtures only for demonstrated gaps.
3. Record current protocol hashes, registry profile, package composition, and test-cost baseline.

### P1 — exact runtime identity

1. Change legacy Action protocol delivery to consume one protocol snapshot without changing its input or output contract.
2. Add a content-addressed immutable public runtime bundle and signed, bounded UTF-8 continuation whose serialized response stays at or below 32 KiB.
3. Extend session identity so protocol and public operational-document drift are both explicit and compatible retained bundles can continue.

### P2 — generated adapter and capability closure

1. Add `project/PUBLIC_PLUGIN_ADAPTER.md` and `project/public-runtime-bindings.json` as the small public transport/capability authority.
2. Add deterministic generation of `skills/askrigor/SKILL.md`, exact packaged project references, initialization guidance, and source/dependency manifests.
3. Add `load_research_runtime` only to the standard-v2 public profile; retain explicit 27-operation legacy and Gemini profiles and controlled internal routes.
4. Extend existing package receipts and generator consistency checks rather than creating a second installation system.

### P3 — demonstrated behavioral repairs

1. Execute A08–A16 against controlled synthetic fixtures.
2. Repair only a failing controller, adapter, or reporting seam; do not duplicate existing clinical/discovery policy text or add fixed remedies/quotas.
3. Re-run focused affected tests after each coherent repair.

### P4 — candidate packaging and rollback

1. Generate and verify source manifests, package closure, compatibility fixtures, and prior-compatible rollback metadata.
2. Exercise disposable local consumers only. Record live/installed acceptance as scoped or access-blocked unless directly observed.
3. Confirm protocol bytes, privacy exclusions, and unrelated packaged skills are unchanged.

### P5 — review boundary

1. Run `npm run verify` once on the final candidate with test-cost observation, then any repository-required policy/security checks.
2. Inspect the exact diff and generated artifacts, bind evidence to the exact head, and open or update the isolated implementation PR with public-safe text.
3. Route the factual receipt through Mission Control if its supported interface is available. Otherwise record the exact missing interface without inventing delivery or review.

## Acceptance ledger

Each case must finish as passed, failed, not run, or access-blocked, with evidence in the task receipt.

| Cases | Planned evidence |
|---|---|
| A01–A03 | generator source-map, package closure, registry/profile fixtures |
| A04–A07 | atomic-load, runtime continuation, legacy compatibility, session-drift tests |
| A08–A09 | diagnostic isolation and distinct admission/error fixtures |
| A10–A16 | discovery, comment candidate, episode, source-scope, transcript, liveness, and count-reconciliation fixtures |
| A17–A18 | package/backend/consumer tuple and privacy scans |
| A19–A20 | direct native Work/Mission Control/fleet evidence, otherwise access-blocked |

## Stop and escalation boundaries

Stop the affected lane for a necessary clinical-policy change, new architecture, spending, production state change, database migration, authentication weakening, frozen-evaluation access, merge-triggered deployment, or unavailable approval/credential. Ordinary code and test defects remain executor work. Independent authorized lanes continue when another lane is blocked.

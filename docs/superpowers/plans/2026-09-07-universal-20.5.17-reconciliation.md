# Universal 20.5.17 reconciliation: PR #191

## Scope and recovery

Owner-authorized outcome: finish the existing PR, preserve its frozen Universal
20.5.17 semantics, repair the current deterministic failure, validate, and merge
when the required checks and repository policy permit. No production deployment
or semantic redesign is part of this repair.

- Starting GitHub head: `e29dd4e2ca8b82f479c99caa1d4e340fb37b232a`.
- Main at recovery: `2f551c762cf1b48b5fbf105b039c68d4f13984e1`.
- Branch: `task/universal-target-preservation-20260907`.
- Isolated checkout: `/tmp/askrigor-pr191-reconciliation`; unrelated work preserved.
- Original failing run/job: `34145384637` / `101816190872`.

## Failure and derivation graph

The exact `npm run verify` failure was reproduced: eight failures in six files,
1,658 passing tests and six existing live-test skips. The first failing assertion
was `tests/epistemic-phase-routing.test.ts:25`:
`expect(universal).toMatch(/version="20\.5\.15" revisionDate="2026-08-24"/u)`.
The other failures were stale root-attribute assertions in the reconstruction
and reinvention tests, stale Universal manifests in two real MCP transports and
the protocol loader, the old published digest rejected with
`Protocol SHA-256 mismatch`, and the current-protocol frontier-import fixture
rejected with `FRONTIER_PROTOCOL_MANIFEST_MISMATCH`.

The actual graph is:

1. Complete `protocols/Universal_Instructions.xml` bytes are canonical.
2. `packages/protocol/src/index.ts` reads those bytes, validates/parses the XML,
   extracts root attributes, and computes SHA-256 on the original byte buffer.
3. MCP manifest/integrity handlers and frontier admission consume that runtime
   manifest. They correctly return/enforce Universal 20.5.17.
4. Current-release test expectations and the README receipt are independently
   maintained exact pins. No repository generator writes them. Their 20.5.15
   values were not advanced with the canonical reconciliation.

The suspected generated-packet defect was disproved before repair. The separate
Custom GPT path is controlled instruction/scout sources and runtime route/tool
definitions → `scripts/generate-custom-gpt-packet.mts` →
`docs/custom-gpt-instructions.md`, `docs/custom-gpt-action-openapi.json`,
`docs/custom-gpt-sync.json`, and
`apps/research-mcp/src/generated/custom-gpt-bundle.ts`. Its inputs do not include
the protocol XML. Both `npm run generate:custom-gpt` and
`npm run generate:action-openapi` completed with no tracked diff. No generator
defect or generated-output edit was needed.

## Repair and preservation

Update the six failing files' current-protocol pins, plus the README current
canonical receipt and its release-packet assertion, in one bounded repair.
Preserve exact digest comparison, malformed-input rejection, mismatch rejection,
all structural content assertions, and historical revision entries. Historical
release receipts and synthetic fixtures with injected protocol identities remain
historical/test data; they are not runtime manifests or generated outputs.

The frozen XML SHA-256 is
`1091bda42dd6e92116dc7a7d4a67d7c1a94c19a891528a7755789485cf5874c1`.
Independent review against both reconciliation parents found valid XML, version
20.5.17 / 2026-09-07, all four owner-specified semantic repairs, no removed
top-level sections, and byte-identical HRP. Main's evidence-direction/specificity
gate and prior checks survive verbatim; the target-preservation line survives
in the reconciled revision and point-of-generation check. Outcome-directed
continuation and strategy switching remain at the forward-motion gate and
point-of-generation checklist. This repair does not change either XML file.

## Completion checklist

- [x] Fetch current PR head and read current applicable instructions.
- [x] Read original hosted failure and reproduce the exact command.
- [x] Map the derivation graph and run the applicable generators.
- [x] Repair stale current-protocol expectations without weakening tests.
- [x] Independently review preservation of both 20.5.16 development lines.
- [x] Run focused protocol/integrity/sync tests and full `npm run verify`.
- [x] Review final diff and complete lesson checkpoint/closeout.

Node 24.18.0 with `npm ci`: the focused protocol, epistemic-phase,
reinvention, reconstruction, MCP, frontier, release-packet, and Custom GPT packet
suite passed all 118 tests across eight files. Full `npm run verify` passed
typecheck, 1,666 tests across 138 files, and build; the six guarded live tests in
one file retained their existing skips. Independent XML parsing and runtime
`verifyProtocolIntegrity` returned the exact frozen identity above. Final review
found no test weakening or omitted current-protocol dependency;
`git diff --check` passed.

Lesson checkpoint at `2026-09-07T18:01:38.825Z`: available, one open candidate,
one needing review, zero accepted awaiting incorporation, four incorporated or
closed, zero deletion eligible. No new lesson submission: this bounded repair
applies the existing canonical-source and exact-byte-integrity requirements;
it introduces no new policy or research finding and does not expand the queue.

## Hosted closeout contract

Push the existing branch, then require the complete hosted deterministic job
(including PostgreSQL living-evidence acceptance/pilot and synthetic Community
Forum/Discourse acceptance), workflow-policy, and all other checks to pass on the
final head. GitHub's current main rules require an up-to-date branch and resolved
review threads, with zero mandatory approving reviews; the initial review-thread
count was zero. Recheck current rules/state, merge PR #191 without bypass when
eligible, and record the resulting head/merge SHA in its description and handoff.

Hosted completion evidence and the final SHA belong to PR #191's description
and GitHub checks, so finalizing this source record does not create a new
post-validation commit merely to restate hosted results.

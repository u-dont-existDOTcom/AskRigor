# MAST Fresh Validation Round 5 execution plan

## Objective

Create, prospectively freeze, execute, judge, score, and report a clean 10-family, 120-response MAST validation successor after Round 4 terminated `INDETERMINATE` at generation-capture admission. Preserve Rounds 2–4 as immutable historical evidence.

## Phase 1 — source-fixed successor and deterministic freeze

1. Fork the exact Round-4 frozen head without editing any Round-4 file or private artifact.
2. Repair the contradictory generation-provider receipt path with one shared strict schema containing `chatMode: TEMPORARY`; use it in both `captureGeneration()` and stored generation capture.
3. Add a regression at the capture-admission consumer seam so a provider receipt containing the required `chatMode` cannot be rejected by an earlier incompatible parser.
4. Exclude Round-4-exposed `Card002`; promote previously semantically untouched `All008`; freeze the resulting ten-family cohort with no remaining reserve.
5. Preserve the four arms, three trials, 120 generations, GPT-5.6 Sol Extra High generation/J1/J2, Latest Pro 5/5 J3 trigger, and the existing P1–P5 decision rule.
6. Reuse and hash-bind the qualified Round-3 Xvfb/systemd Brave/watchdog/profile/loopback-CDP transport.
7. Run focused successor tests, affected transport/runtime tests, typecheck/build, exact hash verification, repository verification, and `git diff --check` once at the freeze checkpoint.
8. Commit, push, open the Round-5 freeze PR, require green checks, and seal the private freeze receipt against the exact reviewed head.

## Phase 2 — deterministic supervision and live normal-path acceptance

1. Install only the hash-bound Round-5 scripts on the dedicated VPS while preserving the unchanged Round-3 browser services/profile.
2. Run deterministic pre-Send/post-Send supervision and no-resend regressions without model calls.
3. Run one tiny non-MAST temporary/unpersonalized GPT-5.6 Sol Extra High synthetic submission through the exact Round-5 capture path, including the repaired `chatMode` provider receipt, and seal it normally.
4. Verify a fresh temporary conversation afterward and a bounded healthy service/CDP soak.
5. Any source change after this acceptance requires a new freeze head and repeated acceptance before benchmark response #1.

## Phase 3 — generation

1. Prepare and transfer 120 private packets with exact source/destination/composer hash and length equality.
2. Generate sequentially through the dedicated browser and one managed ChatGPT content tab.
3. Persist send intent before click and `SENT` immediately after; never automatically resend after Send may have occurred.
4. Resume only sealed successes. Any unresolved post-Send ambiguity terminates the whole study `INDETERMINATE`.

## Phase 4 — blinded evaluation

1. Seal 120/120 generations and build opaque packets plus sealed condition map.
2. Run independent J1 and J2 with GPT-5.6 Sol Extra High.
3. Run Latest Pro 5/5 J3 only for preregistered score-vector disagreements.
4. Preserve malformed/failed artifacts and never rerun a sealed judgment.

## Phase 5 — unblind, score, and report

1. Require complete J1/J2 plus triggered J3 coverage.
2. Join only by opaque IDs and bound hashes.
3. Apply the unchanged 10-family P1–P5 rule with equal denominators.
4. Keep raw score, benchmark conformity, clinical-validity review, and benchmark-target conflicts separate.
5. Emit final PASS / FAIL / INDETERMINATE receipt and preserve all integrity/failure/exposure artifacts.

## Terminal boundaries

Round 4 receives no further prompt. Card002 remains development-exposed. After Round-5 response #1, any material frozen-component change invalidates Round 5. No semantic reserve remains after promoting All008.

# Independent Semantic Representation Review v0.2 candidate

This directory is a prospective DEVELOPMENT-only correction selected by the
frozen v0.1 evaluation. It is not installed in production and contains no new
reviewer performance result.

The v0.2 prompt retains the v0.1 machine submission schema while strengthening
the semantic procedure. A reviewer must evaluate each discriminator against the
actual feature records and stored values for the represented positive and
control cases. Matching words are insufficient: the candidate operator must
preserve the source predicate's truth conditions.

`prospective/20260911-v02-contract/` contains a new 11-pair mutation corpus. The
source-faithful mate now preserves the named exposure, represents an exact timing
fact as exact, and keeps the Quartz hypothesis free of unsupported dose or timing
attachments. The prospective admission receipt blocks reviewer generation until
two independent source/state-only adjudications exist for every candidate and a
third resolves each disagreement.

Historical v0.1 artifacts, labels, and outputs remain unchanged.

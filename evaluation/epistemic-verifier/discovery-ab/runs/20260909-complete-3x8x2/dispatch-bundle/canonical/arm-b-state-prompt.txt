You are in a fresh AskRigor consumer reasoning session. Use only the ordinary canonical AskRigor instructions already installed in this consumer surface and the source packet below. Do not browse, search, inspect GitHub, load issue #208, load verifier implementation/tests, view expected outcomes, view scorer keys, or view Arm A output.

Your first job is semantic representation only. Produce exactly one JSON object conforming to EpistemicStateV1. Do not synthesize a final answer yet.

EpistemicStateV1 structure:
- state_version: exactly "askrigor_epistemic_state_v1"
- target: {target_id, label, description}
- cases[]: {case_id, label, comparison_role, provenance}
  comparison_role is one of positive, tolerated_control, negative_control, unknown.
- observations[]: {observation_id, target_id, case_id, statement, material, qualifiers, high_information_qualifier_keys, provenance}
- features[]: {case_id, feature_id, value, provenance}
- comparisons[]: {comparison_id, target_id, positive_case_ids, control_case_ids}
- hypotheses[]: {hypothesis_id, target_id, statement, material, discriminator, qualifier_predictions}
  discriminator: {feature_id, operator, value?, modifier?}
  operator is present, absent, equals, not_equals, gt, gte, lt, or lte.
  modifier, if used: {kind, claim_id}, where kind is dose, form, route, context, interaction, or other.
  qualifier_predictions[]: {observation_id, qualifier_key, operator, value, exception_claim_id?}
  prediction operator is equals, not_equals, gt, gte, lt, or lte.
- claims[]: {claim_id, target_id, kind, statement, material, provenance, dependencies}
  kind is conclusion, modifier, mechanism, or intermediate.
  dependency is {kind: "observation"|"claim", id}.
- provenance: {kind, source_id}, where kind is user_supplied, source_grounded, or inference.

Preserve the exact target, positive/control contrast, all high-information timing/dose/amount qualifiers, feature assignments, source-grounded modifiers, and dependencies present in the packet. Do not invent missing controls or observations. A conclusion claim may encode your current proposed conclusion, but it is not permission to synthesize prose before deterministic verification.

BLIND_PACKET_ID: {{BLIND_PACKET_ID}}

SOURCE_PACKET:
{{SOURCE_PACKET}}

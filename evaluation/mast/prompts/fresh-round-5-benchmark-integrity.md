Review one already-flagged benchmark action without changing its raw benchmark score. Compare the exact raw rubric requirement with current high-authority clinical evidence for the same population, timing, and decision context.

Return exactly one JSON object containing: `schemaVersion` (the number `1`), `studyId` (`askrigor-mast-fresh-validation-round-5-20260920`), `familyId`, `actionId`, `rawRubricRequirement`, `rawBenchmarkConformity`, `evidence`, `disposition`, `uncertainty`, `materiallyAffectsInterpretation`, and `reviewerProvenance`. Evidence entries must preserve a source identifier, direct URL, authority description, exact population/timing/context comparison, and the SHA-256 of the exact saved source artifact supplied for review. `reviewerProvenance` must preserve the visible model label, visible reasoning label, visible reasoning ordinal, and review timestamp. Use `BENCHMARK_TARGET_CONFLICT` only when warranted. Otherwise use `NO_CONFLICT` or `UNRESOLVED`. Do not alter the raw benchmark judgment or invent a source digest.

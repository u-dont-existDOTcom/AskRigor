name: Apply comparison-integrity protocol patch

on:
  push:
    branches:
      - logic-comparison-integrity-2026-09-08

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Smoke
        run: echo runner-ok

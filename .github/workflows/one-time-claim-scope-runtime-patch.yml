name: One-time claim-scope runtime patch smoke

on:
  push:
    branches:
      - lesson/claim-scope-runtime-20260913

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke
        run: echo workflow-started

name: Premise integrity protocol update

on:
  push:
    branches:
      - codex/premise-integrity-truth-priority-2026-08-16
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: node scripts/apply-premise-integrity-update.mjs
      - run: node scripts/update-premise-integrity-mcp-expectations.mjs
      - run: npm run verify
      - run: git diff --check
      - name: Commit generated canonical protocols
        shell: bash
        run: |
          set -euo pipefail
          git add protocols/Universal_Instructions.xml protocols/HRP_Full.xml tests/protocol.test.ts tests/mcp-tools.test.ts
          if git diff --cached --quiet; then
            echo "No generated changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "protocols: add premise integrity truth priority"
          git push origin "HEAD:${GITHUB_REF_NAME}"
